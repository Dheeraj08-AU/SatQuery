Here is absolutely everything, including the exact source code for every core file, entirely in text format. You can copy this entire message and paste it to Claude.

SatQuery AI - Comprehensive Project State & Codebase Handover
Target: Smart India Hackathon (SIH) 2026 Problem Statement: 26167 (SatQuery AI - Interactive Vision-Language Assistant for Multimodal Remote Sensing) Goal: Build a flawless, robust, and highly efficient agentic architecture that perfectly satisfies the problem statement under strict local hardware constraints (~16GB RAM limit, CPU inference).

1. Agentic Architecture & Routing (app.py)
The frontend is a Streamlit application. We satisfy the "Agentic Orchestration" requirement by utilizing the Gemini API (via google-genai SDK) as a zero-shot router with Structured Outputs. Gemini parses the query, validates inputs, and outputs a strict JSON routing object (task_type, params). The app then triggers the correct specialist model in model_registry.py.

2. The Model Registry & RAM Optimization (model_registry.py)
To avoid OOM (Out of Memory) errors on a 16GB RAM limit, we implemented PEFT Multi-Adapter Switching.

We load a single instance of PaliGemma-3B in bfloat16.
We load multiple task-specific LoRA adapters (e.g., vqa, change) onto this base model using peft.
We dynamically switch the active adapter using model.set_adapter("adapter_name") before inference.
3. Task Status & Immediate Roadmap
Single-Image VQA: DONE. Uses satquery_vqa_adapter fine-tuned on RSVQA/VRSBench.
Bi-Temporal Change Analysis: DONE. We pivoted away from a failed "multi-probe" CDVQA fine-tune. Instead, we use the original satquery_vqa_adapter and pass it a side-by-side composite image (Time 1 and Time 2 pasted horizontally). It perfectly discriminates changes (e.g., "Vegetation" vs "Traffic").
Optical-SAR Fusion: DONE (using synthetic grayscale placeholders). We use the side-by-side composite technique again with the VQA adapter.
Roadmap Task A (WIP): Implement GroundingDINO box_threshold=0.20 filtering in run_grounding to prevent VLM hallucinated objects.
Roadmap Task B (WIP): Pull real co-registered Optical/SAR pairs from HuggingFace dataset Hermanni/sen12mscr (SEN12MS-CR) which natively bundles raw serialized float32 arrays in Parquet format, replacing our synthetic data.
COMPLETE SOURCE CODE
1. app.py (The Agentic Streamlit Frontend)
python
import os
from dotenv import load_dotenv
load_dotenv()
import tempfile
import json
import numpy as np
from PIL import Image, ImageDraw
import streamlit as st
import rasterio
from rasterio.warp import transform_bounds
from modules.geo_validator import SatQueryValidator
from modules.agent_controller import AgentController, TaskType, InputValidationError
st.set_page_config(page_title="SatQuery AI", page_icon="🛰️", layout="wide")
@st.cache_resource
def load_modules():
    return SatQueryValidator(), AgentController()
validator, agent = load_modules()
def save_uploaded_file(uploaded_file) -> str:
    suffix = os.path.splitext(uploaded_file.name)[1]
    with tempfile.NamedTemporaryFile(delete=False, suffix=suffix) as tmp_file:
        tmp_file.write(uploaded_file.getvalue())
        return tmp_file.name
def draw_real_grounding_box(img_path: str, box: list, label: str, confidence: float) -> Image.Image:
    img = Image.open(img_path).convert("RGB")
    if box is None: return img
    draw = ImageDraw.Draw(img)
    draw.rectangle(box, outline="red", width=4)
    draw.text((box[0] + 5, box[1] + 5), f"{label} ({confidence:.2f})", fill="red")
    return img
def draw_mock_change_map(img1_path: str, img2_path: str) -> Image.Image:
    img1 = np.array(Image.open(img1_path).convert("L"))
    img2 = np.array(Image.open(img2_path).convert("L"))
    if img1.shape != img2.shape:
        img2 = np.array(Image.fromarray(img2).resize((img1.shape[1], img1.shape[0])))
    diff = np.abs(img1.astype(int) - img2.astype(int))
    diff_colored = np.zeros((diff.shape[0], diff.shape[1], 3), dtype=np.uint8)
    diff_colored[diff > 40] = [255, 0, 0]
    return Image.fromarray(diff_colored)
def generate_gis_geojson(img_path: str, task_name: str, answer_text: str, confidence: float = 0.0) -> dict:
    geometry = None
    try:
        with rasterio.open(img_path) as src:
            if src.crs:
                left, bottom, right, top = transform_bounds(src.crs, "EPSG:4326", *src.bounds)
                geometry = {"type": "Polygon", "coordinates": [[[left, bottom], [right, bottom], [right, top], [left, top], [left, bottom]]]}
    except: pass
    return {"type": "FeatureCollection", "features": [{"type": "Feature", "geometry": geometry, "properties": {"task": task_name, "description": answer_text, "confidence": round(confidence, 4)}}]}
st.title("🛰️ SatQuery AI")
input_mode = st.sidebar.radio("Select Input Scope:", ["Single Image", "Bi-Temporal Pair (t1, t2)", "Cross-Modal Pair (Optical + SAR)"])
INPUT_MODE_MAP = {"Single Image": "single", "Bi-Temporal Pair (t1, t2)": "bi_temporal", "Cross-Modal Pair (Optical + SAR)": "cross_modal"}
current_input_mode = INPUT_MODE_MAP.get(input_mode, "single")
uploaded_files = []
if input_mode == "Single Image":
    f1 = st.sidebar.file_uploader("Upload Image:", type=["tif", "png", "jpg"])
    if f1: uploaded_files.append((f1, "Single Input Image"))
elif input_mode == "Bi-Temporal Pair (t1, t2)":
    f1 = st.sidebar.file_uploader("Upload Time-1 (t1):", type=["tif", "png", "jpg"], key="t1")
    f2 = st.sidebar.file_uploader("Upload Time-2 (t2):", type=["tif", "png", "jpg"], key="t2")
    if f1 and f2: uploaded_files.extend([(f1, "Time 1"), (f2, "Time 2")])
elif input_mode == "Cross-Modal Pair (Optical + SAR)":
    f1 = st.sidebar.file_uploader("Upload Optical:", type=["tif", "png", "jpg"], key="opt")
    f2 = st.sidebar.file_uploader("Upload SAR:", type=["tif", "png", "jpg"], key="sar")
    if f1 and f2: uploaded_files.extend([(f1, "Optical Image"), (f2, "SAR Image")])
col_left, col_right = st.columns([1, 1])
temp_paths = []
valid_inputs = False
with col_left:
    st.subheader("🖼️ Preview")
    if uploaded_files:
        for ufile, label in uploaded_files:
            tpath = save_uploaded_file(ufile)
            temp_paths.append(tpath)
            st.image(ufile, caption=f"{label}")
        if len(temp_paths) == 2:
            is_coregistered = validator.verify_coregistration(temp_paths[0], temp_paths[1])
            valid_inputs = is_coregistered
            if valid_inputs: st.success("Co-registration Verified")
            else: st.error("Co-registration Failed")
        else: valid_inputs = True
with col_right:
    st.subheader("💬 Query")
    user_query = st.text_input("Enter Natural Language Query:")
    run_btn = st.button("🚀 Analyze", disabled=not (valid_inputs and len(user_query) > 3))
if run_btn:
    trace = agent.route_and_configure(user_query, temp_paths, input_mode=current_input_mode)
    with st.expander("🛠️ Execution Summary", expanded=True):
        st.json({"Task": trace.selected_task.value, "Model": trace.selected_tool, "Status": trace.status})
    
    result_data = trace.execution_result or {}
    answer = result_data.get("answer", str(result_data))
    real_conf = result_data.get("confidence", 0.0) * 100
    st.success(answer)
    
    # Visualization Logic
    if trace.selected_task == TaskType.SINGLE_GROUNDING and temp_paths:
        annotated_img = draw_real_grounding_box(temp_paths[0], result_data.get("box"), result_data.get("label"), result_data.get("confidence", 0.0))
        st.image(annotated_img, caption="Grounding Overlay")
    elif trace.selected_task == TaskType.CHANGE_ANALYSIS and len(temp_paths) == 2:
        st.image(draw_mock_change_map(temp_paths[0], temp_paths[1]), caption="Change Map")
2. modules/model_registry.py (The PEFT Orchestrator)
python
import torch
from PIL import Image
import math
import hashlib
import json
import os
from transformers import AutoProcessor, GroundingDinoForObjectDetection, PaliGemmaForConditionalGeneration
from peft import PeftModel
class ModelRegistry:
    def __init__(self):
        self.cache_file = "vqa_cache.json"
        self.vqa_cache = json.load(open(self.cache_file)) if os.path.exists(self.cache_file) else {}
        self.vlm_processor = None
        self.vlm_model = None
        self.grounding_model = None
    def _ensure_vlm_loaded(self):
        if self.vlm_model is not None: return
        self.vlm_processor = AutoProcessor.from_pretrained("modules/satquery_vqa_adapter")
        base_vlm = PaliGemmaForConditionalGeneration.from_pretrained("google/paligemma-3b-pt-224", torch_dtype=torch.bfloat16, device_map="cpu", low_cpu_mem_usage=True)
        # Load multiple adapters via PEFT
        self.vlm_model = PeftModel.from_pretrained(base_vlm, "modules/satquery_vqa_adapter", adapter_name="vqa")
        self.vlm_model.load_adapter("modules/satquery_change_adapter", adapter_name="change")
        self.vlm_model.eval()
    def run_single_vqa(self, image_path: str, query: str, params: dict) -> dict:
        cache_key = hashlib.md5((open(image_path, "rb").read() + query.encode()).hexdigest())
        if cache_key in self.vqa_cache: return self.vqa_cache[cache_key]
        
        self._ensure_vlm_loaded()
        img = Image.open(image_path).convert("RGB")
        inputs = self.vlm_processor(text=f"answer en {query}", images=img, return_tensors="pt").to(torch.bfloat16)
        inputs["input_ids"] = inputs["input_ids"].to(torch.long)
        inputs["attention_mask"] = inputs["attention_mask"].to(torch.long)
        
        self.vlm_model.set_adapter("vqa") # Switch PEFT Adapter
        
        with torch.no_grad():
            output = self.vlm_model.generate(**inputs, max_new_tokens=50)
            
        answer = self.vlm_processor.decode(output[0][inputs["input_ids"].shape[1]:], skip_special_tokens=True)
        return {"answer": answer, "confidence": 0.95}
    def run_change_analysis(self, img1_path: str, img2_path: str, query: str, params: dict) -> dict:
        self._ensure_vlm_loaded()
        img1 = Image.open(img1_path).convert("RGB")
        img2 = Image.open(img2_path).convert("RGB").resize(img1.size)
        
        # Side-by-side composite
        composite = Image.new('RGB', (img1.width * 2, img1.height))
        composite.paste(img1, (0, 0))
        composite.paste(img2, (img1.width, 0))
        
        prompt = f"answer en The left image is from time T1, the right image is from time T2. Answer the question about what changed: {query}"
        inputs = self.vlm_processor(text=prompt, images=composite, return_tensors="pt").to(torch.bfloat16)
        inputs["input_ids"] = inputs["input_ids"].to(torch.long)
        inputs["attention_mask"] = inputs["attention_mask"].to(torch.long)
        
        self.vlm_model.set_adapter("vqa") # We reverted to VQA adapter because multi-probe failed
        
        with torch.no_grad():
            output = self.vlm_model.generate(**inputs, max_new_tokens=50)
        
        answer = self.vlm_processor.decode(output[0][inputs["input_ids"].shape[1]:], skip_special_tokens=True)
        return {"answer": answer, "confidence": 0.95}
    def run_optical_sar(self, optical_path: str, sar_path: str, query: str, params: dict) -> dict:
        self._ensure_vlm_loaded()
        img1 = Image.open(optical_path).convert("RGB")
        img2 = Image.open(sar_path).convert("RGB").resize(img1.size)
        
        composite = Image.new('RGB', (img1.width * 2, img1.height))
        composite.paste(img1, (0, 0))
        composite.paste(img2, (img1.width, 0))
        
        prompt = f"answer en The left image is optical, the right image is SAR (radar). Using both together: {query}"
        inputs = self.vlm_processor(text=prompt, images=composite, return_tensors="pt").to(torch.bfloat16)
        inputs["input_ids"] = inputs["input_ids"].to(torch.long)
        inputs["attention_mask"] = inputs["attention_mask"].to(torch.long)
        
        self.vlm_model.set_adapter("vqa")
        with torch.no_grad():
            output = self.vlm_model.generate(**inputs, max_new_tokens=50)
            
        answer = self.vlm_processor.decode(output[0][inputs["input_ids"].shape[1]:], skip_special_tokens=True)
        return {"answer": answer, "confidence": 0.95}
    def run_grounding(self, image_path: str, query, params: dict) -> dict:
        # TODO: Implement strict box_threshold=0.20 filter here to reject hallucinations
        pass
3. finetune_vlm.py (The VQA Fine-tuning Script)
python
import torch
from datasets import Dataset
from PIL import Image
from transformers import AutoProcessor, PaliGemmaForConditionalGeneration, BitsAndBytesConfig, TrainingArguments, Trainer
from peft import get_peft_model, LoraConfig
model_id = "google/paligemma-3b-pt-224"
processor = AutoProcessor.from_pretrained(model_id)
bnb_config = BitsAndBytesConfig(load_in_4bit=True, bnb_4bit_quant_type="nf4", bnb_4bit_compute_dtype=torch.bfloat16)
model = PaliGemmaForConditionalGeneration.from_pretrained(model_id, quantization_config=bnb_config, device_map={"": 0})
# Freeze vision encoder
for param in model.vision_tower.parameters(): param.requires_grad = False
for param in model.multi_modal_projector.parameters(): param.requires_grad = False
lora_config = LoraConfig(r=16, lora_alpha=32, target_modules=["q_proj", "k_proj", "v_proj", "o_proj"], lora_dropout=0.05, bias="none", task_type="CAUSAL_LM")
model = get_peft_model(model, lora_config)
def collate_fn(examples):
    texts = [f"answer en {ex['question']}" for ex in examples]
    labels = [ex['answer'] for ex in examples]
    images = [Image.open(ex['image_path']).convert("RGB") for ex in examples]
    inputs = processor(text=texts, images=images, suffix=labels, return_tensors="pt", padding="longest")
    return {"input_ids": inputs["input_ids"], "attention_mask": inputs["attention_mask"], "pixel_values": inputs["pixel_values"], "labels": inputs["labels"]}
training_args = TrainingArguments(output_dir="./paligemma_satquery_adapter", per_device_train_batch_size=2, gradient_accumulation_steps=4, num_train_epochs=2, learning_rate=2e-4, fp16=True, optim="paged_adamw_8bit")
trainer = Trainer(model=model, args=training_args, train_dataset=hf_dataset, data_collator=collate_fn)
trainer.train()
model.save_pretrained("./satquery_vqa_adapter")
processor.save_pretrained("./satquery_vqa_adapter")
5:20 PM

Short answer: it's enough to reason about architecture and strategy, but not enough to fix or extend code. And this machine doesn't have the project — the folder here has a SIH26117 directory (different PS) and a pile of paper-writing scripts, nothing SatQuery.

What the handover gives me

Enough to judge the design and spot problems. I can already see concrete bugs in what you pasted:

model_registry.py:run_single_vqa is broken as written:

cache_key = hashlib.md5((open(image_path,"rb").read() + query.encode()).hexdigest())

.hexdigest() is being called on the bytes object, not on the md5 object → AttributeError on every call. Should be hashlib.md5(...).hexdigest(). Also the cache is read but never written back to disk.

run_grounding is pass → returns None → app.py renders an empty answer for the grounding path. That's one of your two mandatory single-image tasks.

confidence: 0.95 is hardcoded everywhere. The PS explicitly asks for confidence estimation as an evaluated output. Judges will diff three tasks all returning 0.95 and notice. You have the logits — compute it from output_scores / compute_transition_scores.

The GeoTIFF path is the biggest risk. You're doing Image.open(path).convert("RGB") everywhere. That silently fails or garbles on a 12-band Sentinel-2 GeoTIFF and on RISAT SAR (float32/uint16, single band, huge dynamic range). The ISRO/SAC eval set is pre-georeferenced Cartosat-2S + RISAT GeoTIFF pairs. You need a rasterio loader with band selection + percentile stretch before PIL ever sees the data, otherwise you score near-zero on the set that matters most.

Gemini API as the agentic router is a live-internet dependency in the one component the PS says is mandatory. If the eval is offline or rate-limited, your whole system returns nothing. You need a deterministic local fallback router.

What I'd need files for

Anything where I have to match your actual code: modules/agent_controller.py and modules/geo_validator.py (never pasted — I can't see how routing or co-registration actually works), the adapter configs, your dataset prep script, requirements.txt, and whether a benchmark eval harness exists at all (VRSBench/RSVQA/CDVQA test splits — final scoring uses those, and I saw no scoring script).

Suggestion

Bring the repo to this machine (zip it, or push to a private GitHub repo and clone here). Then I can actually edit and run things instead of handing you patches to copy across devices.

Want me to start now on the two things I can do from text alone — a hardened rasterio-based image loader for GeoTIFF/SAR, and real confidence extraction from generation scores — so they're ready to drop in when you get the files over?
