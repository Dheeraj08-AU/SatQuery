# SatQuery AI - training dependencies (Google Colab, T4 GPU).
#
# bitsandbytes is Linux/CUDA only, which is why it is not in the base
# requirements: the Streamlit app must remain installable on Windows.

-r requirements.txt

datasets>=2.20,<4
bitsandbytes>=0.43
sentencepiece>=0.2,<1
